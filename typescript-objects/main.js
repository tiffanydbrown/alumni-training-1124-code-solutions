'use strict';
const student = {
  firstName: 'Tiffany',
  lastName: 'Brown',
  age: 99,
};
const fullName = student.firstName + ' ' + student.lastName;
console.log('The value of fullName:', fullName);
